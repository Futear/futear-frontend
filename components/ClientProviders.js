// "use client";

// import { ScopeModalProvider } from "@/components/scope/ScopeModalContext";
// import ScopeModal from "@/components/scope/ScopeModal";
// import { useSession } from "@/hooks/auth/useSession";

// export default function ClientProviders({ children, scopes }) {
//   useSession();

//   return (
//     <ScopeModalProvider scopes={scopes}>
//       <ScopeModal />
//       {children}
//     </ScopeModalProvider>
//   );
// }
